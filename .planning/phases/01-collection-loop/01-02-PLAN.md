---
phase: 01-collection-loop
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/search.py
  - tests/test_search.py
autonomous: true
requirements: [DATA-01, FILTER-01, FILTER-02, FILTER-03]
must_haves:
  truths:
    - "discover_repos merges topic-union and keyword-fallback results into one dict keyed by str(repo.id) (FILTER-01)"
    - "every query string excludes fork and archived repos; the keyword half adds stars:>=10 while the topic half stays floor-free (FILTER-03)"
    - "BOTH discovery passes (date-windowed discover_repos with NEW_REPO_WINDOWS [7,30] AND star-banded discover_established) check totalCount and narrow/split when it reaches the 900 threshold, keeping every sub-query under the 1,000-result cap and the tracked universe focused (~150-300) (FILTER-02 / Success Criterion 3, D-04, D-05)"
    - "discover_established queries the star bands regardless of creation date, catching old-but-spiking repos (D-11)"
    - "safe_search pre-checks the search rate limit and never references the token value (DATA-01)"
  artifacts:
    - path: "src/search.py"
      provides: "safe_search, build_*_query, discover_repos, discover_established, refresh_tracked"
      contains: "def discover_repos"
    - path: "tests/test_search.py"
      provides: "pure query-builder + cap-handling + merge-by-id tests using mocked search"
      contains: "totalCount"
  key_links:
    - from: "src/search.py"
      to: "src/config.py"
      via: "imports TOPICS, KEYWORD_SETS, KEYWORD_STAR_FLOOR, QUALIFIER_EXCLUSIONS, BREAKTHROUGH_STAR_BANDS, TOTAL_COUNT_CAP_WARN"
      pattern: "from .config import|from src.config import"
    - from: "discover_repos / discover_established"
      to: "candidate dict"
      via: "found[str(repo.id)] = repo"
      pattern: "str\\(repo.id\\)"
---

## Decision Traceability

Locked CONTEXT.md decisions implemented by this plan (literal IDs for the decision-coverage gate):
- D-04: NEW_REPO_WINDOWS = [7, 30] day creation windows drive the focused ~150–300 tracked universe
- D-05: every sub-query is date-windowed and/or star-banded to keep total_count under the 1,000-result cap
- D-11: discover_established runs the star-banded standing query so old-but-spiking repos are caught, not just newly-created ones

<objective>
Build the API discovery layer: the combo AI filter (topic-union + keyword fallback,
FILTER-01), date-windowing under the 1,000-result cap (FILTER-02), fork/archived
exclusion with a keyword-half star floor (FILTER-03), the D-11 star-banded
established-repo pass, and the core re-fetch of tracked repos — all authenticated via
the env-var token (DATA-01).

Purpose: this is the "fetch" half of the fetch→snapshot→commit loop. It must reach the
right repos (combo filter), stay under GitHub's hard caps on EVERY sub-query (both the
date-windowed pass and the star-banded pass), and pace itself against the strict
30 req/min search limit (safe_search).
Output: `src/search.py` with pure query builders + mockable discovery functions, fully
unit-tested without any live network call.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@./CLAUDE.md
@.planning/phases/01-collection-loop/01-RESEARCH.md
@.planning/phases/01-collection-loop/01-CONTEXT.md

<interfaces>
<!-- From src/config.py (Plan 01). Import these directly — do not redefine. -->
```python
TOPICS: list[str]                  # 6 LLM-era topics (D-01)
KEYWORD_SETS: list[list[str]]      # 2 keyword sublists, <=5 terms each (D-02)
KEYWORD_STAR_FLOOR: int            # 10 (D-02)
KEYWORD_IN_QUALIFIER: str          # "in:name,description"
QUALIFIER_EXCLUSIONS: str          # "fork:false archived:false" (both halves)
BREAKTHROUGH_STAR_BANDS: list[str] # ["100..1000", "1000..10000"] (D-11)
NEW_REPO_WINDOWS: list[int]        # [7, 30] creation-window days (D-04)
TOTAL_COUNT_CAP_WARN: int          # 900 (FILTER-02 threshold)
```
<!-- Import style: this module uses `from src.config import ...`. The package root is on -->
<!-- sys.path because the collector is invoked as `python -m src.collector` (Plan 04). -->
<!-- PyGithub client + safe_search contract (RESEARCH Patterns 1 & 2). -->
<!-- The Github client `g` is constructed in Plan 04 (collector.py main) and passed in. -->
<!-- A search result is a PaginatedList with a `.totalCount` int attribute and is iterable -->
<!-- yielding repo objects that expose `.id` (int), `.stargazers_count`, `.full_name`. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure query builders + safe_search + cap-check helper</name>
  <read_first>
    - src/config.py (constants imported here)
    - .planning/phases/01-collection-loop/01-RESEARCH.md Pattern 2 (safe_search), Pattern 4 (topic query), Pattern 5 (keyword OR query), Pattern 5a (star-band query), Pitfall 1 (topic AND), Pitfall 2 (1000 cap)
    - ./CLAUDE.md §GitHub API Constraints (30 search req/min, 1000-result cap)
    - src/search.py (file being created)
  </read_first>
  <files>src/search.py, tests/test_search.py</files>
  <behavior>
    - build_topic_query("llm", since_date="2026-06-20") == "topic:llm fork:false archived:false created:>2026-06-20" (no star floor on topic half — D-03)
    - build_keyword_query(["llm","gpt"], since_date="2026-06-20") == 'llm OR gpt in:name,description stars:>=10 fork:false archived:false created:>2026-06-20' (star floor present — FILTER-03)
    - build_established_query("agents", "100..1000") == "topic:agents stars:100..1000 fork:false archived:false" (NO created: window — D-11; date-independent)
    - over_cap(result_with_totalCount_950) is True; over_cap(result_with_totalCount_800) is False (threshold == TOTAL_COUNT_CAP_WARN, 900)
    - split_star_band("100..1000") returns two narrower contiguous sub-bands covering the same range (e.g. ["100..300", "300..1000"]) with no gap — used to subdivide an over-cap established band
    - safe_search: when mocked get_rate_limit().search.remaining > 0, calls g.search_repositories(query) once and returns its result WITHOUT sleeping; when remaining == 0 then >0 on second poll, it sleeps then proceeds (assert time.sleep was called, patched)
    - safe_search NEVER references a token: assert the string "token" / auth does not appear in any log/print (there are no print statements)
  </behavior>
  <action>
    Create `src/search.py`. Import constants from config (`from src.config import ...`)
    and `import time, warnings`, `from datetime import datetime, timezone`, `import github`.
    Implement PURE builders (no network) returning query strings exactly as in <behavior>:
      - `build_topic_query(topic, since_date)` → topic half, NO star floor (D-03). Uses QUALIFIER_EXCLUSIONS.
      - `build_keyword_query(keywords, since_date)` → ` OR `.join(keywords) + " " + KEYWORD_IN_QUALIFIER + f" stars:>={KEYWORD_STAR_FLOOR} " + QUALIFIER_EXCLUSIONS + f" created:>{since_date}". (FILTER-01/03)
      - `build_established_query(topic, band)` → f"topic:{topic} stars:{band} {QUALIFIER_EXCLUSIONS}" — no creation window (D-11).
      - `since_date_for(window_days, now=None)` → (now or datetime.now(timezone.utc)) minus window_days, formatted "%Y-%m-%d". Inject `now` for deterministic tests.
      - `split_star_band(band)` → parse "LO..HI", compute midpoint, return ["LO..MID", "MID..HI"] (used when an established band exceeds the cap — FILTER-02).
    Implement `over_cap(results) -> bool`: `return results.totalCount >= TOTAL_COUNT_CAP_WARN` (FILTER-02 / Pitfall 2).
    Implement `safe_search(g, query, **kwargs)` EXACTLY per RESEARCH Pattern 2: loop pre-checking `g.get_rate_limit().search.remaining`, sleep to reset (UTC-aware) if 0, then `return g.search_repositories(query, **kwargs)`. Do NOT print, log, or format the token, the `g` client, or auth anywhere.
    Write `tests/test_search.py` covering every <behavior> bullet. For safe_search use a
    fake `g` with `get_rate_limit()` returning an object whose `.search.remaining`
    follows a scripted sequence; patch `time.sleep`. Use a fake results object with a
    `.totalCount` attribute for over_cap. NO real github.Github construction, NO network.
  </action>
  <verify>
    <automated>uv run pytest tests/test_search.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'QUALIFIER_EXCLUSIONS' src/search.py` matches (fork/archived exclusion via constant)
    - `grep -F 'stars:>=' src/search.py` appears only in the keyword builder, not the topic builder (manual: topic builder has no star floor)
    - `grep -F '.totalCount' src/search.py` matches (cap check present)
    - `grep -F 'def split_star_band' src/search.py` matches (band subdivision available)
    - `grep -Eic '(print|logging).*(token|auth| g )' src/search.py` returns 0 (no token/client logging)
    - `uv run pytest tests/test_search.py` all pass
  </acceptance_criteria>
  <done>Query builders, band-splitter, cap check, and rate-paced safe_search are implemented and unit-tested with zero network calls.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: discover_repos + discover_established (combo filter, merge by id, cap handling on BOTH passes)</name>
  <read_first>
    - src/search.py (builders, over_cap, split_star_band, safe_search from Task 1)
    - .planning/phases/01-collection-loop/01-RESEARCH.md Pattern 4, Pattern 5, Pattern 5a, Pitfall 2, Pitfall 5; §Open Questions #3 (D-11 Reading B)
    - .planning/phases/01-collection-loop/01-CONTEXT.md D-01, D-02, D-04, D-11
    - .planning/ROADMAP.md §"Phase 1" Success Criterion 3 (every sub-query total_count < 1,000)
  </read_first>
  <files>src/search.py, tests/test_search.py</files>
  <behavior>
    - discover_repos: given a fake search callable returning repos with ids [1,2] for a topic and [2,3] for a keyword set, the returned dict has string keys {"1","2","3"} — deduped/merged by str(repo.id) (FILTER-01). Same id from two queries appears once.
    - discover_repos issues one query per topic (6) for EACH new-repo window in NEW_REPO_WINDOWS, plus one query per KEYWORD_SET per window — assert the injected search callable was invoked with topic-half queries that contain no "stars:>=" and keyword-half queries that contain "stars:>=10".
    - When a query's result.totalCount >= 900, discover_repos hits a narrowing path (tightens the created: window to the smallest window OR emits warnings.warn) rather than silently paginating — assert the warn/narrow branch is taken. (FILTER-02)
    - discover_established: iterates BREAKTHROUGH_STAR_BANDS x TOPICS (2x6=12 queries) with date-independent queries; merges by str(repo.id). Returns repos regardless of creation date (D-11).
    - discover_established cap-handling: when a band's result.totalCount >= 900, it calls split_star_band and re-queries the two sub-bands (assert the two narrower queries are issued) instead of silently truncating. (FILTER-02 — Success Criterion 3 applies to the breakthrough pass too)
  </behavior>
  <action>
    Add to `src/search.py`:
      - `discover_repos(g, windows=NEW_REPO_WINDOWS, search=safe_search) -> dict[str, repo]`:
        for each window compute `since = since_date_for(window)`, then for each topic call
        `search(g, build_topic_query(topic, since))` and for each keyword set call
        `search(g, build_keyword_query(kw_set, since))`; merge every repo via
        `found[str(repo.id)] = repo` (Pitfall 5 — merge, never overwrite the dict wholesale).
        After each search, if `over_cap(results)`: `warnings.warn(...)` AND re-issue with a
        tighter `created:` window for that slice (smallest window in NEW_REPO_WINDOWS).
        Make `search` an injected parameter so tests pass a fake (no network).
      - `discover_established(g, search=safe_search) -> dict[str, repo]`:
        for band in BREAKTHROUGH_STAR_BANDS, for topic in TOPICS, call
        `search(g, build_established_query(topic, band))`; merge by `str(repo.id)`. If
        `over_cap(results)`: for each sub-band in `split_star_band(band)`, re-issue
        `search(g, build_established_query(topic, sub_band))` and merge (FILTER-02). (D-11 / Pattern 5a)
    Extend `tests/test_search.py` with a fake search callable (records queries, returns
    scripted repo lists with `.id` ints and a `.totalCount`) covering every <behavior>
    bullet, including the established-pass band-split path. No network, no real PaginatedList.
  </action>
  <verify>
    <automated>uv run pytest tests/test_search.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'def discover_repos' src/search.py` and `grep -F 'def discover_established' src/search.py` match
    - `grep -c 'str(repo.id)' src/search.py` is >= 2 (merge by numeric id in both discovery functions)
    - `grep -F 'over_cap' src/search.py` appears in BOTH discover_repos and discover_established bodies (cap handling on both passes)
    - `grep -F 'split_star_band' src/search.py` is referenced inside discover_established
    - test asserting merged dict keys == {"1","2","3"} passes
    - test asserting 12 established queries (2 bands x 6 topics) issued passes
    - test asserting an over-cap band triggers two split-band re-queries passes
  </acceptance_criteria>
  <done>Combo filter merges by numeric id; BOTH the date-windowed and star-banded passes handle the 1000-result cap; the D-11 pass runs date-independently — all proven by mocked tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: refresh_tracked (core re-fetch of previously tracked repos)</name>
  <read_first>
    - src/search.py (existing functions)
    - .planning/phases/01-collection-loop/01-RESEARCH.md Pattern 3 (get_repo by int), Pattern 6 (refresh_tracked), Pitfall 6 (get_topics extra call)
    - ./CLAUDE.md §GitHub API Constraints (core 1000 req/hr)
  </read_first>
  <files>src/search.py, tests/test_search.py</files>
  <behavior>
    - refresh_tracked(g, ["111","222"]) calls g.get_repo(111) and g.get_repo(222) (int args — Pattern 3) and returns {"111": repo, "222": repo} keyed by str(repo.id)
    - When g.get_repo raises github.UnknownObjectException for an id, that id is skipped (not in result) and no exception propagates (Pattern 6)
    - The exception handler does NOT log the exception's full repr or the client (token-safety) — only the repo id string may be referenced
  </behavior>
  <action>
    Add `refresh_tracked(g, tracked_ids: list[str]) -> dict[str, repo]` to `src/search.py`
    per RESEARCH Pattern 6: loop the ids, `repo = g.get_repo(int(rid))`, store
    `refreshed[str(repo.id)] = repo`; wrap in `try/except github.UnknownObjectException`
    and `continue` on failure. Do NOT call `repo.get_topics()` in this loop (Pitfall 6 —
    extra API call per repo). Do NOT log the exception object or the client `g`; if you
    log a skip, reference only `rid`. `tracked_ids` is supplied by the caller (Plan 03's
    load_metadata_ids returns them) — refresh_tracked does no file IO.
    Extend `tests/test_search.py` with a fake `g` whose `get_repo` returns objects with
    `.id` and raises `github.UnknownObjectException` for one id. No network.
  </action>
  <verify>
    <automated>uv run pytest tests/test_search.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'def refresh_tracked' src/search.py` matches
    - `grep -F 'get_repo(int(' src/search.py` matches (numeric-id fetch — Pattern 3)
    - `grep -F 'UnknownObjectException' src/search.py` matches (deleted/private repos handled)
    - `grep -v '^[[:space:]]*#' src/search.py | grep -c 'get_topics()'` returns 0 (no extra topic call in code, comments excluded — Pitfall 6)
    - test for skipped-deleted-repo and keyed-by-str-id passes
  </acceptance_criteria>
  <done>refresh_tracked re-fetches tracked repos by numeric id, skips deleted ones safely, leaks no token data — proven by mocked tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| collector process → GitHub Search/Core API | The authenticated Github client `g` carries the token in its auth header |
| GitHub API response → in-memory candidate dict | Public repo metadata (untrusted text) flows in; later serialized to JSON |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Information Disclosure | safe_search / refresh_tracked exception + log paths | mitigate | Never `print`/`log` the client `g`, the auth, or a full exception repr (which can embed request headers). Logs reference only the query string or repo id. Verified by grep gate in each task. |
| T-01-05 | Denial of Service | unbounded pagination / rate-limit exhaustion | mitigate | `safe_search` pre-checks `search.remaining` and sleeps to reset; PyGithub `GithubRetry` (from Plan 04 client) handles 403s on later pages; `over_cap` (on BOTH discovery passes) prevents silently spinning past the 1000-result cap (Pitfall 2). |
| T-01-06 | Tampering | malicious repo description/full_name from API | accept | Phase 1 only holds these in memory and serializes via `json.dumps` (escaped) in Plan 03; no shell/markdown interpolation occurs until Phase 2, which owns its own escaping. |
</threat_model>

<verification>
- `uv run pytest tests/test_search.py -q` passes with zero network access.
- Grep confirms topic half has no star floor, keyword half has `stars:>=10`, both have `fork:false archived:false`.
- Grep confirms `over_cap` is wired into BOTH discover_repos and discover_established.
- Grep confirms no token/client/exception-repr logging in search.py.
</verification>

<success_criteria>
- Combo filter (FILTER-01) merges topic + keyword results by `str(repo.id)`.
- Every sub-query — date-windowed AND star-banded — is cap-checked at the 900 threshold and narrowed/split (FILTER-02 / Success Criterion 3).
- Fork/archived excluded everywhere; star floor only on keyword half (FILTER-03).
- D-11 star-banded established pass runs date-independently.
- All proven by mocked unit tests — no live API call in the suite.
</success_criteria>

<output>
After completion, create `.planning/phases/01-collection-loop/01-02-SUMMARY.md`
</output>
