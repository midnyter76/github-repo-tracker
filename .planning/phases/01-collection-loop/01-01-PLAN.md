---
phase: 01-collection-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .python-version
  - .gitignore
  - src/__init__.py
  - src/config.py
  - tests/__init__.py
  - tests/test_config.py
  - data/snapshots/.gitkeep
autonomous: true
requirements: [FILTER-04]
must_haves:
  truths:
    - "config constants are importable and contain the 6 locked LLM-era topics (D-01)"
    - "keyword star floor is 10 and two breakthrough star bands are defined (D-02, D-11)"
    - "pytest runs against the project via uv"
  artifacts:
    - path: "src/config.py"
      provides: "TOPICS, KEYWORD_SETS, KEYWORD_STAR_FLOOR, BREAKTHROUGH_STAR_BANDS, NEW_REPO_WINDOWS, path + cap constants"
      contains: "TOPICS"
    - path: "pyproject.toml"
      provides: "PyGithub==2.9.1 runtime dep + pytest dev dep"
      contains: "PyGithub==2.9.1"
    - path: ".python-version"
      provides: "Python 3.12 pin"
      contains: "3.12"
  key_links:
    - from: "src/config.py"
      to: "src/search.py + src/store.py (later plans)"
      via: "module-level constant imports"
      pattern: "from .config import"
---

<objective>
Establish the greenfield project scaffold and the single source of truth for all
tunable filter constants (FILTER-04). This is the foundation every later plan imports.

Purpose: D-03/D-04 lock the topic list, keyword sets, star floor, and breakthrough
star bands as CONFIGURABLE CONSTANTS — never inline. Centralizing them now means
Plans 02/03/04 import from one place, and post-launch tuning touches one file.
Output: Installable uv project (pyproject.toml, uv.lock, .python-version), a
`src/config.py` constants module, a runnable pytest harness, and the committed
`data/snapshots/` directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@./CLAUDE.md
@.planning/phases/01-collection-loop/01-CONTEXT.md
@.planning/phases/01-collection-loop/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize uv project, pin Python 3.12, add deps</name>
  <read_first>
    - ./CLAUDE.md §Technology Stack (locked versions: Python 3.12, PyGithub 2.9.1, uv)
    - ./CLAUDE.md §Installation (uv init / uv add commands)
    - .planning/phases/01-collection-loop/01-RESEARCH.md §Standard Stack → Installation
  </read_first>
  <files>pyproject.toml, uv.lock, .python-version, .gitignore</files>
  <action>
    Use uv (mandated by CLAUDE.md — do NOT hand-write requirements.txt or use pip).
    From the repo root run, in order:
      1. `echo "3.12" > .python-version`
      2. `uv add "PyGithub==2.9.1"` — generates/updates pyproject.toml + uv.lock with the pinned runtime dependency.
      3. `uv add --dev pytest` — adds pytest to the dev dependency group.
    If `uv init` complains about an existing directory, initialize in place; the goal
    is a valid `pyproject.toml` with `requires-python = ">=3.12"`, a `[project]`
    dependency `PyGithub==2.9.1`, a dev group containing `pytest`, and a committed
    `uv.lock`. uv.lock MUST be committed (reproducible CI) — do not gitignore it.
    Create `.gitignore` containing exactly these lines: `.venv/`, `__pycache__/`,
    `*.pyc`, `.pytest_cache/`. Do NOT add `.env` handling logic anywhere — the token
    lives only in `os.environ` at runtime (RESEARCH §Security Domain); there is no
    secrets file to ignore.
  </action>
  <verify>
    <automated>uv run python -c "import github; print(github.__version__)" && test -f uv.lock && grep -q "3.12" .python-version</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` contains the string `PyGithub==2.9.1` (grep -F "PyGithub==2.9.1" pyproject.toml)
    - `pyproject.toml` references `pytest` in a dev/optional dependency group (grep -i pytest pyproject.toml)
    - `.python-version` content is `3.12`
    - `uv.lock` exists and is NOT listed in `.gitignore`
    - `.gitignore` contains `.venv/` and `__pycache__/`
  </acceptance_criteria>
  <done>uv project installs, PyGithub 2.9.1 importable, pytest available, lock committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write src/config.py constants module (FILTER-04)</name>
  <read_first>
    - .planning/phases/01-collection-loop/01-CONTEXT.md §Decisions D-01, D-02, D-03, D-04, D-11
    - .planning/phases/01-collection-loop/01-RESEARCH.md Pattern 4 (TOPICS), Pattern 5 (KEYWORDS/floor), Pattern 5a (BREAKTHROUGH_STAR_BANDS), Pattern 9 (paths)
    - src/config.py (the file being created)
  </read_first>
  <files>src/__init__.py, src/config.py, tests/__init__.py, tests/test_config.py</files>
  <behavior>
    - Test: TOPICS equals exactly the 6 D-01 strings in order: ["llm", "large-language-models", "agents", "rag", "generative-ai", "llmops"]
    - Test: KEYWORD_STAR_FLOOR == 10 (D-02)
    - Test: BREAKTHROUGH_STAR_BANDS == ["100..1000", "1000..10000"] (D-11 / Pattern 5a) and has length 2
    - Test: NEW_REPO_WINDOWS contains both 7 and 30 (D-04 weekly+monthly creation windows)
    - Test: TOTAL_COUNT_CAP_WARN == 900 and is < 1000 (Pitfall 2 / FILTER-02 threshold)
    - Test: QUALIFIER_EXCLUSIONS == "fork:false archived:false" (D-02/D-03/FILTER-03)
    - Test: SNAPSHOTS_DIR is a Path ending in "data/snapshots"; METADATA_PATH ends in "data/metadata.json"
  </behavior>
  <action>
    Create `src/__init__.py` (empty) and `tests/__init__.py` (empty).
    Create `src/config.py` with ONLY module-level constants (no logic, no API calls):
      - `TOPICS = ["llm", "large-language-models", "agents", "rag", "generative-ai", "llmops"]` (D-01, exact strings/order)
      - `KEYWORD_SETS = [["llm", "gpt", "langchain", "transformer", "chatgpt"], ["openai", "claude", "gemini", "\"language model\"", "rag"]]` (D-02; each sublist <= 5 terms to respect the 5-operator query cap — RESEARCH Pattern 5)
      - `KEYWORD_STAR_FLOOR = 10` (D-02)
      - `KEYWORD_IN_QUALIFIER = "in:name,description"` (FILTER-01)
      - `QUALIFIER_EXCLUSIONS = "fork:false archived:false"` (D-03 / FILTER-03 — applies to BOTH halves)
      - `BREAKTHROUGH_STAR_BANDS = ["100..1000", "1000..10000"]` (D-11 / Pattern 5a)
      - `NEW_REPO_WINDOWS = [7, 30]` (D-04 — last-7d weekly + last-30d monthly creation windows; comment each)
      - `TOTAL_COUNT_CAP_WARN = 900` (FILTER-02 — warn/narrow threshold below the hard 1000 cap)
      - `from pathlib import Path` then `SNAPSHOTS_DIR = Path("data/snapshots")` and `METADATA_PATH = Path("data/metadata.json")` (Pattern 9)
    Add a short module docstring stating these are the FILTER-04 tunable constants per D-03.
    Then write `tests/test_config.py` covering every assertion in <behavior> above using plain `assert` statements that import from `src.config`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_config.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"llm"' src/config.py` finds the topic; `python -c "from src.config import TOPICS; assert len(TOPICS)==6"` passes
    - `grep -F 'KEYWORD_STAR_FLOOR = 10' src/config.py` matches
    - `grep -F '"100..1000"' src/config.py` and `grep -F '"1000..10000"' src/config.py` both match
    - `grep -F 'fork:false archived:false' src/config.py` matches
    - tests/test_config.py exists and `uv run pytest tests/test_config.py` reports all passed
  </acceptance_criteria>
  <done>config.py holds every locked constant (D-01/02/03/04/11) and test_config.py passes green.</done>
</task>

<task type="auto">
  <name>Task 3: Commit the snapshots data directory</name>
  <read_first>
    - .planning/phases/01-collection-loop/01-RESEARCH.md §Recommended Project Structure + Pattern 7 (SNAPSHOTS_DIR)
  </read_first>
  <files>data/snapshots/.gitkeep</files>
  <action>
    Create `data/snapshots/.gitkeep` (empty file) so the per-date snapshot directory
    (Pattern 7 / Pattern 9) exists in git from day 1 and the first scheduled run can
    write `data/snapshots/YYYY-MM-DD.json` without a missing-directory failure. Do NOT
    create `data/metadata.json` here — it is written by the collector at runtime
    (DATA-03, Plan 03).
  </action>
  <verify>
    <automated>test -f data/snapshots/.gitkeep && test ! -f data/metadata.json</automated>
  </verify>
  <acceptance_criteria>
    - `data/snapshots/.gitkeep` exists (test -f passes)
    - `data/metadata.json` does NOT exist yet (test ! -f passes)
  </acceptance_criteria>
  <done>data/snapshots/ is tracked in git via .gitkeep; no runtime files pre-created.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer machine → committed repo | Scaffold files are committed; must not introduce a secrets file or commit a token |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | pyproject/.gitignore scaffold | mitigate | No `.env` or secrets file is created; token never touches disk in any phase (RESEARCH §Security Domain). `.gitignore` covers build artifacts only. |
| T-01-02 | Tampering | config.py constants | accept | Constants are code-defined literals (no external/user input), so no injection surface; query strings built from them are validated in Plan 02. |
| T-01-03 | Elevation | dependency supply chain | mitigate | Pin `PyGithub==2.9.1` exactly and commit `uv.lock` for reproducible, audited installs (no floating versions). |
</threat_model>

<verification>
- `uv run pytest -q` passes (test_config green).
- `uv run python -c "import github"` succeeds (PyGithub installed from lock).
- No file in the repo contains a literal token value (grep for `ghp_` / `github_pat_` returns nothing).
</verification>

<success_criteria>
- Installable uv project with committed `uv.lock` and `.python-version` = 3.12.
- `src/config.py` exposes every FILTER-04 constant locked by D-01/02/03/04/11.
- pytest harness runs; `data/snapshots/` exists in git.
</success_criteria>

<output>
After completion, create `.planning/phases/01-collection-loop/01-01-SUMMARY.md`
</output>
