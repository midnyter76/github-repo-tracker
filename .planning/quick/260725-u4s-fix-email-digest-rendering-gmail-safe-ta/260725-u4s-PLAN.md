---
task_id: 260725-u4s
type: quick-full
plan: 01
wave: 1
depends_on: []
autonomous: true
files_modified:
  - src/report.py
  - src/rank.py
  - tests/test_report.py
  - tests/test_rank.py
requirements: [F1-gmail-flex, F2-tracked-count, F3-brand-new-dedupe, F4-star-glyph]

must_haves:
  truths:
    - "The rendered HTML digest contains zero flexbox declarations (display:flex, justify-content, align-items, flex:1, flex-shrink, margin-left:auto) — every layout that previously relied on flex is a table."
    - "The hero card's velocity number, 'stars / day' label, and right-aligned '★ N · age' sit on one horizontal line in a table row, with the star/age cell right-aligned via align=\"right\"."
    - "Each repo row renders its velocity gutter (78px, right-aligned) beside the name/description/bar column as two cells of one table."
    - "The REPOS TRACKED stat shows the true count of repos in today's snapshot (thousands), not the <=35 rendered-bucket count."
    - "compute_buckets still returns exactly four top-level keys — collector.py's `for b in buckets.values() ... b['entries']` iteration is unbroken."
    - "A repo present in both brand_new_weekly and brand_new_monthly contributes 1 to the BRAND NEW stat, not 2."
    - "The third stats-strip label reads 'TOP ★/DAY' with U+2605."
    - "The markdown digest output is byte-for-byte unchanged."
  artifacts:
    - path: "src/report.py"
      provides: "Table-based render_html_hero/render_html_row/render_html_bucket/render_html_digest; tracked_total-aware _count_tracked; set-based _count_brand_new; ★ glyph"
      contains: "tracked_total"
    - path: "src/rank.py"
      provides: "tracked_total surfaced on each of the four bucket dicts"
      contains: "tracked_total"
    - path: "tests/test_report.py"
      provides: "Updated HTML markup assertions + >=4 new tests, one per finding"
    - path: "tests/test_rank.py"
      provides: "tracked_total contract test + four-top-level-keys guard"
  key_links:
    - from: "src/rank.py compute_buckets"
      to: "src/report.py _count_tracked"
      via: "tracked_total key on each bucket dict (NOT a new top-level key)"
      pattern: "tracked_total"
    - from: "src/report.py render_html_leaders"
      to: "_render_strip_cell REPOS TRACKED"
      via: "str(_count_tracked(buckets))"
      pattern: "REPOS TRACKED"
    - from: "src/collector.py:165"
      to: "buckets.values()"
      via: "unchanged four-key iteration — must not regress"
      pattern: "for b in buckets.values"
---

<objective>
Fix four defects in the HTML email digest found in a joint Claude + Codex audit:
Gmail-stripped `display:flex` layouts (F1), a REPOS TRACKED stat wrong by two
orders of magnitude (F2), double-counted BRAND NEW (F3), and a stray `*` where a
`★` belongs (F4).

Purpose: the digest is delivered as HTML email; Gmail strips flexbox, so the hero
and repo rows currently collapse to a vertical stack and right-aligned elements
lose alignment. The stats strip reports numbers that are simply wrong.

Output: `src/report.py` HTML path converted to the `<table role="presentation">`
pattern already proven in this file; `tracked_total` threaded from
`rank.compute_buckets`; updated + new tests in `tests/test_report.py` and
`tests/test_rank.py`.

Scope discipline: this is a rendering-robustness change, NOT a redesign. Colors,
fonts, sizes, spacing values, and cell content stay identical. No new modules, no
new dependencies, no templating library.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@src/report.py
@src/rank.py
@tests/test_report.py

Prior art (read the CODE, not the plans — the pattern is already in this file):
- `src/report.py:526-578` — `_render_leader_cell` / `_render_strip_cell`. These
  are the exact Gmail-safe patterns to reuse: `<table role="presentation"
  cellpadding="0" cellspacing="0">`, `valign="top"|"middle"|"bottom"` instead of
  `align-items`, `width=` / `align="right"` attributes instead of `flex:1` and
  `margin-left:auto`. Quick tasks 260701-ibb / 260701-j1w / 260701-u16 built
  these; do not invent a second pattern.

Test helpers already in `tests/test_report.py`: `_entry()`, `_active_bucket()`,
`_inactive_bucket()`, `_make_buckets()`, `_now()` (lines 28-93). Follow the
existing style: import the function under test inside the test body.

<interfaces>
<!-- Contracts the executor needs. Do not go looking for these in the codebase. -->

Bucket contract returned by `rank.compute_buckets` (src/rank.py:392-417) — each
of the FOUR keys maps to:
```python
{
    "active": bool,
    "snapshots_available": int,
    "window_target": int,
    "entries": [ {id, full_name, html_url, description, created_at, stars, velocity_per_day}, ... ],
}
```

CRITICAL — callers that iterate the buckets dict (verified by grep, this is the
complete list):
- `src/collector.py:165` — `[e["id"] for b in buckets.values() for e in b["entries"]]`
  → a new TOP-LEVEL int key would raise `TypeError: 'int' object is not subscriptable`.
- `tests/test_rank.py:891` — `for bucket_name, bucket in buckets.items(): bucket["entries"]`
  → same failure mode.
- `src/report.py:333, 509, 520` and `write_digest` (`_SECTIONS`) — iterate a fixed
  key list, unaffected either way.

Therefore F2 puts `tracked_total` INSIDE each bucket dict (alongside
`snapshots_available`), never at the top level. That breaks zero callers.

Relevant local names in `src/rank.py compute_buckets`:
```python
snaps = load_snapshots(snapshots_dir)   # list of snapshot dicts
n_snaps = len(snaps)
current = snaps[-1] if snaps else None  # today's snapshot; current["repos"] is the id->data dict
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thread the true tracked count from rank.compute_buckets to the stats strip (F2)</name>
  <files>src/rank.py, src/report.py, tests/test_rank.py, tests/test_report.py</files>
  <behavior>
    - `compute_buckets` output: every one of the four bucket dicts carries
      `tracked_total == len(current["repos"])`.
    - `compute_buckets` with zero snapshots: `tracked_total == 0` on all four buckets.
    - `set(compute_buckets(...).keys())` is still exactly the four bucket names.
    - `_count_tracked(buckets)` returns `tracked_total` when the key is present.
    - `_count_tracked(buckets)` falls back to the distinct-id count across the four
      rendered buckets when the key is absent (old-shaped dicts must not crash).
    - `render_html_leaders` renders the `tracked_total` value next to "REPOS TRACKED".
  </behavior>
  <action>
**src/rank.py:**
1. In `compute_buckets`, after `current = snaps[-1] if snaps else None` (line 288),
   add: `tracked_total = len(current["repos"]) if current is not None else 0`.
2. Add `"tracked_total": tracked_total,` to each of the FOUR bucket dicts in the
   return statement (lines 392-417), next to `"snapshots_available"`.
   Do NOT add a top-level key — see `<interfaces>` for why.
3. Update the bucket-contract docstring block (lines 248-254) to include
   `"tracked_total": N` in the shape, with a one-line note that it is the count of
   repos in today's snapshot (total tracked), distinct from the per-bucket entry counts.

**src/report.py:**
4. Rewrite `_count_tracked` (lines 507-510):
   ```python
   def _count_tracked(buckets: dict) -> int:
       """Total repos tracked today (rank.compute_buckets surfaces `tracked_total`
       on each bucket). Falls back to the distinct-id count across the four
       rendered buckets when the key is absent (pre-tracked_total bucket dicts)."""
       total = buckets["brand_new_weekly"].get("tracked_total")
       if total is not None:
           return total
       ids = {str(e["id"]) for key, _kicker, _title in _HTML_SECTIONS for e in buckets[key]["entries"]}
       return len(ids)
   ```
   `render_html_leaders` already calls `_count_tracked(buckets)` for the REPOS
   TRACKED cell — no call-site change needed.

**tests/test_report.py:**
5. Add an optional `tracked_total: int | None = None` parameter to `_active_bucket`,
   `_inactive_bucket`, and `_make_buckets`; the key is inserted into the bucket dict
   ONLY when the value is not None. Default None keeps every existing test on the
   fallback path unchanged.
6. Retitle/re-document the existing `TestCountTracked::test_dedupes_same_id_across_buckets`
   (line 832) as the explicit fallback test — it is now proving the no-key branch.
   Keep its `== 3` assertion.
7. Add `TestCountTracked::test_uses_tracked_total_when_present`:
   `_make_buckets(tracked_total=2211)` → `_count_tracked(...) == 2211` even though
   the four buckets hold at most a handful of entries.
8. Add `TestHtmlDigestStatsStrip::test_repos_tracked_shows_true_total_not_bucket_count`:
   render `render_html_leaders(_make_buckets(tracked_total=2211), markers={}, now=_now())`
   and assert `>2211<` (the strip value span) appears immediately before the
   "REPOS TRACKED" label — use the same `re.search` value+label pairing style as
   `test_brand_new_stat_is_white_not_green` (line 1023).

**tests/test_rank.py:**
9. Add a test asserting all four buckets carry `tracked_total == len(today's snapshot repos)`
   for a fixture with a known repo count, and a guard asserting
   `set(buckets.keys()) == {"brand_new_weekly", "brand_new_monthly", "spike_24h", "velocity_30d"}`
   (the collector-iteration contract). Reuse an existing `_setup`-style fixture in
   that file rather than building a new one.

Do NOT touch `src/collector.py` — it needs no change and that is the point.
  </action>
  <verify>
    <automated>PYTHONUTF8=1 .venv/Scripts/python.exe -m pytest -q</automated>
    <automated>PYTHONUTF8=1 .venv/Scripts/python.exe -m pytest -q tests/test_rank.py tests/test_report.py -k "tracked"</automated>
    <automated>PYTHONUTF8=1 .venv/Scripts/python.exe -c "import src.collector" </automated>
  </verify>
  <done>
Full suite green at >= 343 passed (339 baseline + >= 4 new). `_count_tracked`
returns the snapshot repo count when `tracked_total` is present and the old
distinct-id count when absent. `compute_buckets` still returns exactly four
top-level keys. `src/collector.py` unmodified. Commit:
`fix(report): show true tracked-repo count in HTML digest stats strip`
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Dedupe BRAND NEW across the two brand-new buckets, fix the star glyph (F3, F4)</name>
  <files>src/report.py, tests/test_report.py</files>
  <behavior>
    - A repo id present in BOTH `brand_new_weekly` and `brand_new_monthly` (marked
      new, or absent from markers) contributes exactly 1 to `_count_brand_new`.
    - An id marked `"returning"` contributes 0.
    - Ids in `spike_24h` / `velocity_30d` still contribute 0 regardless of marker.
    - The rendered digest contains the label `TOP ★/DAY` and not `TOP */DAY`.
  </behavior>
  <action>
**src/report.py:**
1. Rewrite `_count_brand_new` (lines 513-523) to collect qualifying ids into a set
   and return `len(...)`. Keep the existing marker rule verbatim
   (`markers.get(str(e["id"]), "new") == "new"` — absent marker means new) and the
   same two-bucket scope:
   ```python
   ids = {
       str(e["id"])
       for key in ("brand_new_weekly", "brand_new_monthly")
       for e in buckets[key]["entries"]
       if markers.get(str(e["id"]), "new") == "new"
   }
   return len(ids)
   ```
   Update the docstring's first line to say "Distinct" brand-new-flagged repos and
   note that weekly/monthly overlap is intentional (a repo created 3 days ago is in
   both), which is why the dedupe is required.
2. Line 622: change the label string `"TOP */DAY"` to `"TOP ★/DAY"` (U+2605 BLACK
   STAR), matching every other `★` in this file. It routes through `html.escape()`
   in `_render_strip_cell`, which passes non-ASCII through unchanged — no entity
   encoding needed.

**tests/test_report.py:**
3. Add `TestCountBrandNew::test_repo_in_both_brand_new_buckets_counts_once`: the same
   `_entry(id="1")` object placed in both `weekly_entries` and `monthly_entries` plus
   one distinct id → `_count_brand_new(...) == 2`, not 3. Assert on the pre-fix
   failure mode explicitly in the docstring.
4. Update `TestHtmlDigestStatsStrip::test_stats_strip_labels_present` (line 1006):
   `assert "TOP ★/DAY" in result` and `assert "TOP */DAY" not in result`.
   The file already contains `★` literals in test assertions (e.g. line 910), so
   encoding is a non-issue; the run uses `PYTHONUTF8=1`.
5. Verify the existing `test_counts_new_only_in_weekly_and_monthly` (line 851) still
   asserts `== 2` — its ids are already distinct, so it must pass unchanged.
  </action>
  <verify>
    <automated>PYTHONUTF8=1 .venv/Scripts/python.exe -m pytest -q</automated>
    <automated>PYTHONUTF8=1 .venv/Scripts/python.exe -m pytest -q tests/test_report.py -k "brand_new or strip_labels"</automated>
  </verify>
  <done>
Full suite green (>= 344 passed). A repo in both brand-new buckets counts once.
`TOP ★/DAY` renders; `TOP */DAY` appears nowhere in the output. Commit:
`fix(report): dedupe BRAND NEW count and restore star glyph in stats strip`
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Convert hero card and repo rows from flex to tables (F1, part 1)</name>
  <files>src/report.py, tests/test_report.py</files>
  <behavior>
    - `render_html_hero(_entry(), ...)` output contains no `display:flex`,
      `align-items`, `margin-left:auto`, `flex:1`, or `flex-shrink`.
    - Hero stats line: velocity number, "stars / day", and "★ N · age" are three
      cells of one `<table role="presentation">` row; the star/age cell carries
      `align="right"`.
    - `render_html_row(...)` output contains none of the same flex tokens.
    - Repo row: 78px velocity gutter and the name/desc/bar column are two cells of
      one table row; the bar and the "★ N · age" span are two cells of a nested row
      with the star cell right-aligned.
    - Every existing visual value survives: font families/sizes/weights, all colors
      (#34d399, #f4f4f5, #9ca3af, #5b6573, #52525b), `width:78px`, the 16px gutter
      gap, the 7px hero label gap, the 12px bar gap, `padding-bottom:5px`,
      `margin-top:20px`/`8px`/`3px`/`6px`, `padding:12px 0`, `border-top:1px solid #16181d`,
      bar `height:4px` + `border-radius:3px` + `{bar_fill}` + `width:{bar_pct}%`.
    - The NEW badge still renders only when the marker rule says new.
  </behavior>
  <action>
Reuse the `_render_leader_cell` / `_render_strip_cell` table idiom verbatim:
`<table role="presentation" width="100%" cellpadding="0" cellspacing="0">`,
`valign=` instead of `align-items:`, `width=` / `align="right"` attributes instead
of `flex:1` / `margin-left:auto`. Flex-gap margins that were added by quick task
260701-ibb become cell padding or stay as span margins — either is fine as long as
the pixel value is preserved.

**src/report.py `render_html_hero` (lines 378-382)** — replace the stats `<div
style="display:flex; align-items:flex-end; margin-top:20px;">` with:
- one `<table role="presentation" width="100%" cellpadding="0" cellspacing="0" style="margin-top:20px;">`
- td 1: `width="1" valign="bottom"` + `white-space:nowrap`, holding the 52px
  velocity `{vel_fmt}` with its existing font/color/line-height/letter-spacing.
- td 2: `width="1" valign="bottom"` + `white-space:nowrap`, holding `stars / day`
  with its existing styles; the old `margin-left:7px` becomes `padding-left:7px` on
  this cell (same 7px). Keep `padding-bottom:5px`.
- td 3: `align="right" valign="bottom"`, holding `★ {stars_full} · {age}` with its
  existing font/size/color and `padding-bottom:5px`. This cell absorbs the slack —
  that is the `margin-left:auto` replacement.
`align-items:flex-end` → `valign="bottom"` on all three cells.

**src/report.py `render_html_row` (lines 420-438)** — keep the outer `<a>` but
change `display:flex` to `display:block` (retain `text-decoration:none; padding:12px 0;
border-top:1px solid #16181d;`). Inside it:
- one `<table role="presentation" width="100%" cellpadding="0" cellspacing="0">` with
  two cells:
  - td 1: `width="78" valign="top" align="right"` with `width:78px; padding-right:16px;`
    (replaces `flex-shrink:0; width:78px; text-align:right; margin-right:16px`),
    containing the two existing divs (velocity number, "stars / day" label) unchanged.
  - td 2: `valign="top"` (replaces `flex:1`), containing:
    - the name line: DROP `display:flex; align-items:center` from that div entirely
      and leave it a plain block — the repo-name span and the NEW badge are inline
      spans that already flow on one line, and the badge already carries
      `margin-left:9px`. No table needed here; this is the minimum diff.
    - the description div unchanged.
    - the bar line: replace `<div style="display:flex; align-items:center; margin-top:8px;">`
      with a nested `<table role="presentation" width="100%" cellpadding="0" cellspacing="0" style="margin-top:8px;">`:
      - td A: `width="100%" valign="middle"` holding the bar container div with
        `flex:1` replaced by `width:100%` (keep `height:4px; background:#1a1d24;
        border-radius:3px; overflow:hidden;` and the inner fill div verbatim).
      - td B: `width="1" align="right" valign="middle"` with `padding-left:12px`
        (replaces `margin-left:12px`), holding the `★ {stars_full} · {age}` span with
        its existing styles including `white-space:nowrap`.

Do not renumber, reformat, or restyle anything else in these two functions. The
escaping calls (`html.escape(..., quote=True)`, `_esc`) stay exactly as they are.

**tests/test_report.py — update the assertions this markup change breaks:**
- `test_hero_stat_row_has_explicit_margin_not_gap` (line 782): its regex
  `<div style="display:flex; align-items:flex-end;([^"]*)">` no longer matches.
  Rewrite it to assert the 7px separation now lives on the "stars / day" cell
  (`padding-left:7px`) and that `gap:` is still absent.
- `test_row_stat_block_has_explicit_margin_not_gap` (line 766): its `width:78px` div
  regex must become the 78px `<td>`; assert the 16px separation
  (`padding-right:16px`) survives and `gap:16px` is still absent.
Keep both tests' original intent (the 260701-ibb no-gap regression guard) — do not
delete them.

**Add per-function guards:**
- `TestHtmlDigest::test_hero_has_no_flex_declarations`: render `render_html_hero(_entry(),
  "Brand New This Week", _now())` and assert none of
  `display:flex`, `align-items:`, `margin-left:auto`, `flex:1`, `flex-shrink` appear.
- `TestHtmlDigest::test_row_has_no_flex_declarations`: same token list against
  `render_html_row(_entry(), markers={}, bucket_max_vel=1.0, now=_now())`.
- `TestHtmlDigest::test_hero_star_age_cell_is_right_aligned`: assert the
  `★ {stars} · {age}` text sits in a `<td` carrying `align="right"`.
  </action>
  <verify>
    <automated>PYTHONUTF8=1 .venv/Scripts/python.exe -m pytest -q</automated>
    <automated>PYTHONUTF8=1 .venv/Scripts/python.exe -m pytest -q tests/test_report.py -k "hero or row"</automated>
  </verify>
  <done>
Full suite green (>= 347 passed). `render_html_hero` and `render_html_row` emit no
flex tokens; hero star/age and row star/age are right-aligned table cells; the
78px velocity gutter is a `<td width="78" align="right">`. Every color, font,
size, and spacing value from the pre-change markup is still present. Commit:
`fix(report): convert hero card and repo rows to Gmail-safe table layout`
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Convert bucket header, page wrapper and masthead to tables; add whole-document flex guard (F1, part 2)</name>
  <files>src/report.py, tests/test_report.py</files>
  <behavior>
    - `render_html_digest(_make_buckets(), markers={}, now=_now())` contains ZERO
      occurrences of `display:flex`, `display:grid`, `justify-content`,
      `align-items`, `flex:1`, `flex-shrink`, `margin-left:auto`, `gap:`.
    - The 620px card is horizontally centered via `align="center"` on a table cell
      plus `margin:0 auto` on the card div (not `justify-content:center`).
    - "Issue No. N" is left-aligned and the date is right-aligned as two cells of one
      table row (not `justify-content:space-between`).
    - The bucket header still shows kicker, a 1px horizontal rule filling the middle,
      and the right-hand count label, on one line.
    - The markdown digest output is byte-for-byte unchanged (no `_SECTIONS`,
      `render_entry`, `render_bucket`, `write_digest`, or `sanitize_description` edit).
  </behavior>
  <action>
**src/report.py `render_html_bucket` header (lines 488-493)** — replace
`<div style="display:flex; align-items:baseline;">` with a
`<table role="presentation" width="100%" cellpadding="0" cellspacing="0">` row of
three cells:
- td 1: `width="1" valign="middle"` + `white-space:nowrap`, the kicker span with its
  existing styles including `margin-right:12px`.
- td 2: `width="100%" valign="middle"`, holding
  `<div style="height:1px; background:#20222a; font-size:0; line-height:0;"></div>`
  (replaces the `flex:1; height:1px; background:#20222a` span; `font-size:0;
  line-height:0;` stops clients from inflating an empty block to a text line).
- td 3: `width="1" align="right" valign="middle"` + `white-space:nowrap`, the
  `{count_label}` span with its existing styles including `margin-left:12px`.
Keep the outer `<div style="margin-top:34px;">`, the title div, and `{body}` exactly
as they are.

Note the one accepted deviation: `align-items:baseline` has no table equivalent, so
the rule uses `valign="middle"` — up to ~2px off the text baseline. This is strictly
closer to the design than what ships today (Gmail currently collapses the whole row
and drops the rule onto its own line). Do not invent a magic padding constant to
chase the last 2px.

**src/report.py `render_html_digest` outer wrapper (line 684)** — replace
`<div style="width:100%; padding:40px 0; display:flex; justify-content:center;">`
with the standard email centering table:
```html
<table role="presentation" width="100%" cellpadding="0" cellspacing="0" style="background:#d8dadc;">
<tr><td align="center" style="padding:40px 0;">
```
and close with `</td></tr></table>` where the wrapper `</div>` was (line 699). Add
`margin:0 auto;` to the existing 620px card div's style — `align="center"` alone
does not center a fixed-width block child in every client. Every other value on the
card div (`width:620px`, background, border, radius, overflow, box-shadow) is unchanged.

**src/report.py masthead (lines 687-689)** — replace
`<div style="display:flex; justify-content:space-between; align-items:baseline; ...">`
with a `<table role="presentation" width="100%" cellpadding="0" cellspacing="0">`
row of two cells. Email clients do not reliably inherit font styles into `<td>`, so
put the full existing declaration string
(`font-family:'IBM Plex Mono', monospace; font-size:11px; letter-spacing:0.14em;
text-transform:uppercase; color:#5b6573;`) on BOTH cells:
- td 1: `valign="bottom"` holding `<span>Issue No. {issue_no}</span>`.
- td 2: `align="right" valign="bottom"` holding the date span; keep its
  `margin-left:16px` (the 260701-ibb fallback separation).

**tests/test_report.py:**
- Update `test_masthead_issue_no_and_date_have_explicit_spacing` (line 719): the
  two spans are no longer adjacent siblings. Keep the test's intent — assert the date
  span still carries an explicit `margin`, assert its cell carries `align="right"`,
  and keep the "no bare `</span><span>` concatenation" guard.
- Update `test_bucket_header_kicker_and_count_have_margin_not_gap` (line 735): the
  kicker/count `margin-right:12px` / `margin-left:12px` assertions must still pass
  (keep them); replace the `header_div_match` regex with the equivalent check against
  the new header table (no `gap:12px` anywhere in the header markup).
- Add `TestHtmlDigest::test_render_html_digest_has_no_flex_declarations_anywhere`:
  render the full digest and loop-assert that NONE of
  `["display:flex", "display:grid", "justify-content", "align-items", "flex:1",
  "flex-shrink", "margin-left:auto", "gap:"]` appear, naming the offending token in
  the assertion message. This is the F1 acceptance test — it must cover the whole
  document, not one function.
- Add `TestHtmlDigest::test_card_is_centered_by_table_cell_not_flex`: assert
  `align="center"` appears on a `<td` and `margin:0 auto` on the `width:620px` div.
- Keep `test_render_html_digest_has_no_gap_declarations_anywhere` (line 798) as-is —
  the new test supersedes but does not replace it.

FINAL CHECK before commit: `git diff` must show zero changes to
`sanitize_description`, `render_warming_note`, `render_entry`, `render_bucket`,
`_SECTIONS`, and `write_digest`. The markdown path is untouched.
  </action>
  <verify>
    <automated>PYTHONUTF8=1 .venv/Scripts/python.exe -m pytest -q</automated>
    <automated>PYTHONUTF8=1 .venv/Scripts/python.exe -m pytest -q tests/test_report.py</automated>
    <automated>PYTHONUTF8=1 .venv/Scripts/python.exe -c "from datetime import datetime,timezone; import sys; sys.path.insert(0,'.'); from src.report import render_html_digest; b={k:{'active':True,'snapshots_available':5,'window_target':7,'tracked_total':2211,'entries':[{'id':'1','full_name':'o/r','html_url':'https://x','description':'d','created_at':'2026-06-01T10:00:00+00:00','stars':500,'velocity_per_day':12.5}]} for k in ('brand_new_weekly','brand_new_monthly','spike_24h','velocity_30d')}; h=render_html_digest(b,{},datetime(2026,6,28,12,tzinfo=timezone.utc)); bad=[t for t in ('display:flex','display:grid','justify-content','align-items','flex:1','flex-shrink','margin-left:auto','gap:') if t in h]; print('FLEX TOKENS:',bad); assert not bad; assert 'TOP ★/DAY' in h; assert '2211' in h; print('OK')"</automated>
  </verify>
  <done>
Full suite green (>= 349 passed). The rendered digest contains zero flex/grid/gap
tokens end to end. Card centering is `align="center"` + `margin:0 auto`. Masthead
date is right-aligned via `align="right"`. `git diff` confirms the markdown digest
functions are untouched. Commit:
`fix(report): convert digest wrapper, masthead and bucket headers to tables`
  </done>
</task>

</tasks>

<verification>
Run after all four tasks:

1. `PYTHONUTF8=1 .venv/Scripts/python.exe -m pytest -q` → all pass, count >= 349
   (339 baseline + >= 10 new tests; no test deleted).
2. Task 4's inline render check → prints `OK` (zero flex tokens, `TOP ★/DAY`
   present, true tracked count rendered).
3. `git diff HEAD~4 -- src/report.py` → no hunk touches `sanitize_description`,
   `render_warming_note`, `render_entry`, `render_bucket`, `_SECTIONS`, or
   `write_digest`.
4. `git status` → `data/` and `.github/workflows/daily.yml` unmodified.
</verification>

<success_criteria>
- [ ] Zero `display:flex` / `justify-content` / `align-items` / `flex:1` /
      `flex-shrink` / `margin-left:auto` / `gap:` in the rendered HTML digest
- [ ] REPOS TRACKED shows today's snapshot repo count (thousands), sourced from
      `tracked_total` on the bucket dicts
- [ ] `compute_buckets` returns exactly four top-level keys; `src/collector.py`
      unmodified
- [ ] A repo in both brand-new buckets counts once toward BRAND NEW
- [ ] `TOP ★/DAY` renders; `TOP */DAY` gone
- [ ] Markdown digest output byte-for-byte identical
- [ ] Full suite green before every commit; four commits, one per task
- [ ] No new modules, no new dependencies, no colors/fonts/sizes/spacing changed
</success_criteria>

<out_of_scope>
Locked user decisions — do not revisit:
- No test email sent, no browser screenshot taken. Correctness is established by
  unit tests plus the fact that the table pattern is already proven in this file.
  The user confirms Gmail rendering on the next scheduled 13:00 UTC send.
- F2 shows the TRUE tracked count. Do not "fix" it by relabeling the stat.
- Do not modify anything under `data/`.
- Do not change the email-sending step in `.github/workflows/daily.yml`.
</out_of_scope>

<output>
After completion, create
`.planning/quick/260725-u4s-fix-email-digest-rendering-gmail-safe-ta/260725-u4s-SUMMARY.md`
and add the task row to the STATE.md Quick Tasks Completed table.
</output>
</content>
</invoke>
